# tlsaudit - target list example
#
# Format:  host[:port]  [proxy_url]
#
#   host[:port]   Hostname, host:port, or full URL. Port defaults to 443.
#   proxy_url     Optional. HTTP CONNECT proxy for this target only.
#                 Overrides the global --proxy flag.
#                 Omit to use --proxy, or connect directly if --proxy not set.
#
# Blank lines and lines starting with # are ignored.

# ---------------------------------------------------------------------------
# Public endpoints — direct connection
# ---------------------------------------------------------------------------
google.com
github.com
cloudflare.com:443

# ---------------------------------------------------------------------------
# Non-standard HTTPS ports
# ---------------------------------------------------------------------------
example.com:8443
api.example.com:4443

# ---------------------------------------------------------------------------
# Full URLs (scheme and port parsed automatically)
# ---------------------------------------------------------------------------
https://example.com
https://api.example.com:8443

# ---------------------------------------------------------------------------
# Internal services — per-target proxy
# Useful when different segments route through different proxies
# ---------------------------------------------------------------------------
api.internal.example.com:443    http://proxy1.corp.com:8080
auth.internal.example.com:443   http://proxy1.corp.com:8080
legacy.dmz.example.com:8443     http://proxy2.corp.com:443

# ---------------------------------------------------------------------------
# Mixed: some targets need a proxy, some do not
# Global --proxy (if set) applies to lines with no proxy column
# ---------------------------------------------------------------------------
public-api.example.com
restricted.example.com:443      http://restricted-proxy.corp.com:3128
