# TLS Audit - example target list
# Format: host[:port] [proxy_url]
#
# - host[:port]  required  hostname or URL; port defaults to 443
# - proxy_url    optional  per-target HTTP CONNECT proxy; overrides --proxy
#
# Lines starting with # and blank lines are ignored.

# No proxy (uses global --proxy if supplied, otherwise direct)
google.com
github.com
cloudflare.com

# Explicit ports
google.com:443
github.com:443

# Full URLs
https://example.com

# Per-target proxy (overrides --proxy for this line only)
api.internal.corp.com:443 http://proxy1.corp.com:8080
legacy.internal.corp.com:8443 http://proxy2.corp.com:443

# Non-standard ports (badssl.com test endpoints)
tls-v1-2.badssl.com:1012
tls-v1-0.badssl.com:1010
