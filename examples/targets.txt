# tlsaudit - target list example
#
# Format:  host[:port]  [true|false]
#
#   host[:port]   Hostname, host:port, or full URL. Port defaults to 443.
#   true          Route this target through --proxy (default when omitted).
#   false         Bypass --proxy for this target; connect directly.
#
# Blank lines and lines starting with # are ignored.
# The proxy URL itself is set at runtime with --proxy.

# ---------------------------------------------------------------------------
# Use proxy (explicit or by default)
# ---------------------------------------------------------------------------
api.internal.example.com:443
auth.internal.example.com:443   true
legacy.dmz.example.com:8443     true

# ---------------------------------------------------------------------------
# Bypass proxy — connect directly even when --proxy is set
# ---------------------------------------------------------------------------
public-api.example.com          false
cloudflare.com                  false
github.com                      false

# ---------------------------------------------------------------------------
# Non-standard ports and full URLs
# ---------------------------------------------------------------------------
example.com:8443
https://api.example.com:4443
